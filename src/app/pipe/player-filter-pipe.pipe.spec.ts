import { PlayerFilterPipePipe } from './player-filter-pipe.pipe';

describe('PlayerFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
